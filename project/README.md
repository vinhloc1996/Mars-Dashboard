# Mars Dashboard Project

Look up the Mars Rovers information (data, latest photos taken, ...)

## How to run
On the `project` folder, run `yarn install` then `yarn start`. Go to http://localhost:3000 for the data
